/* Dependencies installed by the dsh profile, never committed. */
node_modules/

/* Local noise */
*.log
.DS_Store
Thumbs.db

/* Local credentials and provider configuration. Never commit these files. */
.env
.env.*
!.env.example
*.local.json
